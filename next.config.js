/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // output: "export",
  basePath: "/glsoft-timer",
  "images": {
    unoptimized: true,
  },
  // distDir: 'dist',
}

export default nextConfig
